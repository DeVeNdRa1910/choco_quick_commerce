/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.pexels.com', 
      'aceternity.com',
      'images.unsplash.com',
      'www.shutterstock.com',
    ], 
  },
};

export default nextConfig;
